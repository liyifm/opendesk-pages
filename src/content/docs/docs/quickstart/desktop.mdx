---
title: "OpenDesk Desktop | 快速开始"
description: "OpenDesk Desktop 桌面版快速开始"
---

## 1. 安装 OpenDesk Desktop

OpenDesk Desktop 提供图形化界面，无需安装 NodeJS 等运行环境。

### 1.1 下载安装包

从 [Release Notes](/news/) 页面获取对应平台的最新安装包：

| 平台 | 安装方式 |
|------|----------|
| **macOS** | 下载 `.dmg` 安装包，拖入 Applications |
| **Windows** | 下载 `.exe` 安装包，运行安装向导 |
| **HarmonyOS PC** | 下载`.hap`安装包， 通过通过 Deveco Studio 或 Auto-installer 手动签名后，使用`hdc install xxx.hap`命令安装|

> 安装包和平台差异说明持续更新中，请关注 [Release Notes](/news/) 获取最新版本。

### 1.2 首次启动

安装完成后双击 OpenDesk 图标启动应用。首次启动时，需要配置模型相关信息。

---

## 2. 快速完成初始配置

首次启动 OpenDesk Desktop 时，如果尚未配置模型，会直接弹出配置面板（如上图所示）。面板左侧列出配置项，依次完成即可开始使用。

<img src="/assets/images/quickstart/firsttime_gui.jpg" alt="Desktop 首次配置界面" />

配置面板分为三个部分，从上到下依次操作：

### 模型服务商配置

点击「添加服务商」，选择你使用的模型服务商（如 OpenAI、硅基流动、智谱等），然后填入：

- **API 地址** — 服务商提供的 API Base URL
- **API Key** — 你的 API 密钥

> OpenDesk 支持所有提供 OpenAI 标准模型服务的厂商，填入对应的 API 地址和 Key 即可。

### 模型配置

在「模型配置」区域添加你想使用的具体模型：

- 输入模型的全名（如 `gpt-4o`、`claude-3-opus` 等）
- 为模型设置一个简短别名，方便日常切换

### 默认模型配置

在「默认模型配置」中选择刚才添加的模型作为默认模型，这样每次新建任务都会自动使用它。

---

## 3. 界面概览

启动后你会看到三栏布局：

| 区域 | 功能 |
|------|------|
| **侧边栏** | 任务列表，支持搜索，底部为设置入口 |
| **对话区** | 核心交互区：任务标题栏 + 消息列表 + 输入框 |
| **结果区** | 右侧面板：展示生成的文件、执行步骤、文件变更；可展开/收起，也可导出为文件 |

---

## 4. 创建你的第一个任务

用一句话描述需求即可。试试这样对它说：

- 「查看我今天的日程安排」
- 「帮我把这份销售数据做成分析报告」
- 「读一下这个项目的代码，帮我理清架构」

<img src="/assets/images/manual/quickstart_task.gif" alt="创建第一个任务" />

### 选择工作目录（可选）

1. 点击输入框正上方「当前工作空间」按钮
2. 选择当前任务要使用的目录
3. OpenDesk 仅在该目录内操作文件，不会影响其他位置

### 补充上下文（可选）

| 方式 | 说明 |
|------|------|
| 粘贴截图 | `Ctrl/Cmd + V` 粘贴剪贴板中的图片 |
| 上传文件 | 拖拽文件到输入框 |
| 补充说明 | 明确目标、范围、约束和预期输出 |

---

## 5. 查看结果与追问

任务完成后，右侧结果区包含三类视图：

| 视图 | 内容 |
|------|------|
| **产物视图** | 任务生成的文件（文档、数据、图片等） |
| **变更视图** | 文件的新增/删除/修改（diff 高亮对比） |
| **记忆视图** | 任务过程中自动维护的结构化记忆 |

追问无需重复背景，直接说改什么即可：

- 「再加一个饼图展示占比」
- 「导出为 PDF」

---

## 进一步了解

| 主题 | 说明 |
|------|------|
| [Desktop 使用手册](/docs/manual/desktop/) | 完整的 Desktop 功能介绍 |
| [功能指南](/docs/manual/desktop/tools/) | 各项能力详细说明 |
| [技能系统](/docs/manual/desktop/skills/) | 邮件、日程、笔记等扩展能力 |
| [使用案例](/docs/manual/desktop/use-cases/) | 真实场景示例 |
| [常见问题](/docs/manual/desktop/faq/) | 疑问解答 |
| [CLI 快速开始](/docs/quickstart/cli) | CLI 版安装和使用指南 |